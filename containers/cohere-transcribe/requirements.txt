# Pinned in the Dockerfile RUN; this file is here for `pip install -r`
# convenience when running the server outside the container (rare).
transformers>=4.57,<5
fastapi==0.135.3
uvicorn==0.44.0
soundfile==0.13.1
librosa==0.11.0
numpy==2.4.4
